export default {
    SET_FEEDBACK(state, array) {
        state.feedback = array;
    },
    SET_FEEDBACKS(state, array) {
        state.feedbacks = array;
    },
    SET_ENTRIES(state, array) {
        state.entries = array;
    },
    SET_SINGLE_METRIC(state, array) {
        state.single_metric = array;
    },
}
